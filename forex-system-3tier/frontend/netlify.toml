[build]
  publish = "."

# After deploying backend on Render, replace the hostname below.
[[redirects]]
  from = "/api/*"
  to = "https://YOUR-RENDER-SERVICE.onrender.com/api/:splat"
  status = 200
  force = true
